<%- include('./partials/html-head') %>
<%- include('./partials/nav') %>

<div class="header">
  <h1>Deep Sky</h1>
</div>

<main>
  <h3>Welcome, <%= user ? user.profile.name : "friend" %>.</h3>
  <% if (user?.profile.avatar) { %>
    <div>
      <img 
        src="<%= user.profile.avatar %>" 
        alt="<%= user.profile.name %> avatar"
        id="avatar"
      >
    </div>
  <% } %>
</main>

<%- include('./partials/footer') %>
